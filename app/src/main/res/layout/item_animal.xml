<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="animal"
            type="com.br.julianovincedecampos.animals.model.Animal" />

        <variable
            name="listener"
            type="com.br.julianovincedecampos.animals.view.AnimalClickListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/animalLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{listener::onClick}"
        android:tag="@{animal.name}">

        <ImageView
            android:id="@+id/animalImageView"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:imageUrl="@{animal.imageUrl}"
            android:scaleType="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            style="@style/Title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#afff"
            android:gravity="center"
            android:padding="4dp"
            android:text="@{animal.name}"
            app:layout_constraintBottom_toBottomOf="@+id/animalImageView"
            app:layout_constraintEnd_toEndOf="@+id/animalImageView"
            app:layout_constraintStart_toStartOf="@+id/animalImageView" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
